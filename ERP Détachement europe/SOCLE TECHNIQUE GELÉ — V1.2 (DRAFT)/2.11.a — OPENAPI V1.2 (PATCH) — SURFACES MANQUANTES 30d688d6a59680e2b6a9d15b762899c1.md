# 2.11.a — OPENAPI V1.2 (PATCH) — SURFACES MANQUANTES

- Statut: DRAFT
- Type: DERIVED / DRAFT — does not override LOCKED V1
- Portee: formaliser les surfaces contractuelles manquantes V1.2.1, sans logique metier.

## Sources de reference (lecture seule)

- OpenAPI LOCKED V1: `ERP Détachement europe/SOCLE TECHNIQUE GELÉ — V1 (LOCKED)/2.11 — OPENAPI V1 (PARCOURS MVP) — 1 → 3 → 2 30a688d6a59680b8a5cfc33e26910adb.md`
- Events LOCKED V1: `ERP Détachement europe/SOCLE TECHNIQUE GELÉ — V1 (LOCKED)/2.10 EVENTS MÉTIER V1 (Event-driven, Outbox, IA-fr 30a688d6a5968047ad41e7305d7d9b39.md`
- RBAC LOCKED V1: `ERP Détachement europe/SOCLE TECHNIQUE GELÉ — V1 (LOCKED)/2.12 — RBAC & PERMISSIONS (MATRIX) — V1 30a688d6a596805ea41ae2abb55cbb97.md`
- DB LOCKED V1: `ERP Détachement europe/SOCLE TECHNIQUE GELÉ — V1 (LOCKED)/2.9 - Schéma DB V1 1 (V1 + Patch) 30a688d6a59680edbcb1f475916f9e0a.md`

## Decisions OWNER appliquees (V1.2.1)

1. Auth via Supabase Auth: aucun endpoint `/v1/auth/login` custom.
2. Files: endpoint generique `/v1/files` + endpoints de linking `file_links`.
3. Check events: endpoint unique `POST /v1/check-events` avec `event_type` enum `check_in|check_out`.

## Auth model (Supabase)

- L'authentification est assuree par Supabase Auth.
- L'API metier consomme un JWT valide.
- Toutes les routes ci-dessous exigent `Authorization: Bearer`.
- Le patch V1.2.1 ne cree pas d'endpoints auth custom.

## Patch summary

| Domaine | Endpoints V1.2.1 |
| --- | --- |
| Identity / tenant | `GET /v1/me`, `GET /v1/users`, `POST /v1/users`, `PATCH /v1/users/{user_id}`, `POST /v1/users/{user_id}:deactivate`, `POST /v1/users/{user_id}:reactivate`, `GET /v1/tenant-settings`, `PATCH /v1/tenant-settings` |
| Files | `POST /v1/files`, `GET /v1/files/{file_id}`, `POST /v1/files/{file_id}:soft-delete`, `POST /v1/file-links`, `DELETE /v1/file-links/{file_link_id}` |
| Worker checks | `POST /v1/check-events` |

## Endpoint definitions (contract surfaces)

### 1) GET `/v1/me`

- Request schema: none
- Response schema: `MeResponse`
- Related Events (2.10): none (read route)

### 2) GET `/v1/users`

- Request schema: none
- Response schema: `UserListResponse`
- Related Events (2.10): none (read route)

### 3) POST `/v1/users`

- Request schema: `UserCreateRequest`
- Response schema: `UserCreateResponse`
- Related Events (2.10): `UserCreated`

### 4) PATCH `/v1/users/{user_id}`

- Request schema: `UserPatchRequest`
- Response schema: `UserResponse`
- Related Events (2.10): `UserRoleChanged` si mutation de role incluse.
- OWNER ARBITRATION REQUIRED: confirmer si `role_type` est autorise via ce endpoint ou reserve a une surface dediee.

### 5) POST `/v1/users/{user_id}:deactivate`

- Request schema: none
- Response schema: `UserResponse`
- Related Events (2.10): CONTRACT GAP: event name not confirmed in 2.10 pour changement statut user.

### 6) POST `/v1/users/{user_id}:reactivate`

- Request schema: none
- Response schema: `UserResponse`
- Related Events (2.10): CONTRACT GAP: event name not confirmed in 2.10 pour changement statut user.

### 7) GET `/v1/tenant-settings`

- Request schema: none
- Response schema: `TenantSettingsResponse`
- Related Events (2.10): none (read route)

### 8) PATCH `/v1/tenant-settings`

- Request schema: `TenantSettingsPatchRequest`
- Response schema: `TenantSettingsResponse`
- Related Events (2.10): `TenantSettingsUpdated`

### 9) POST `/v1/files`

- Request schema: `FileUploadRequest`
- Response schema: `FileUploadResponse`
- Related Events (2.10): `FileUploaded`

### 10) GET `/v1/files/{file_id}`

- Request schema: none
- Response schema: `FileResponse`
- Related Events (2.10): `FileAccessed`

### 11) POST `/v1/files/{file_id}:soft-delete`

- Request schema: none
- Response schema: `FileSoftDeleteResponse`
- Related Events (2.10): `FileSoftDeleted`

### 12) POST `/v1/file-links`

- Request schema: `FileLinkCreateRequest`
- Response schema: `FileLinkResponse`
- Related Events (2.10): CONTRACT GAP: event name not confirmed in 2.10 pour creation de file_link.

### 13) DELETE `/v1/file-links/{file_link_id}`

- Request schema: none
- Response schema: `FileLinkResponse`
- Related Events (2.10): CONTRACT GAP: event name not confirmed in 2.10 pour suppression de file_link.

### 14) POST `/v1/check-events`

- Request schema: `CheckEventCreateRequest`
- Response schema: `CheckEventCreateResponse`
- Related Events (2.10): `WorkerCheckEventRecorded`
- Notes: V1 sans geolocalisation; `event_type` est le switch unique check-in/check-out.

### 15) POST `/v1/missions/{mission_id}/incidents`

  body: { type: absence|accident|noncompliance|other, occurred_at, description }
  auth: tenant_admin, agency_user
  → Event: MissionIncidentReported { mission_id, type, occurred_at }

### 15) GET `/v1/missions/{mission_id}/incidents`

  auth: tenant_admin, agency_user, consultant (scoped)


## API Mobile Worker (M7bis)

Endpoints lecture mobile :

GET /v1/worker/missions              — Missions actives du worker authentifié
GET /v1/worker/missions/{id}         — Détail mission + statut conformité visible
GET /v1/worker/documents             — Documents du worker + statuts + expirations
GET /v1/worker/missions/{id}/remuneration  — Rémunération & indemnités déclarées (V1 : saisie, pas snapshot)

Règle : lecture only, tenant-scoped, worker role, aucun calcul côté mobile.

## Schema stubs (minimal placeholders)

### MeResponse

- `user_id`: uuid
- `tenant_id`: uuid
- `role`: string

### UserListResponse

- `items`: array of `UserResponse`
- `next_cursor`: string nullable

### UserCreateRequest

- `email`: string
- `role`: string
- `is_active`: boolean

### UserCreateResponse

- `user_id`: uuid
- `tenant_id`: uuid

### UserPatchRequest

- `email`: string optional
- `role`: string optional
- `is_active`: boolean optional

### UserResponse

- `user_id`: uuid
- `tenant_id`: uuid
- `email`: string
- `role`: string
- `is_active`: boolean

### TenantSettingsResponse

- `tenant_id`: uuid
- `settings`: object

### TenantSettingsPatchRequest

- `settings`: object

### FileUploadRequest

- `filename`: string
- `mime_type`: string
- `size_bytes`: integer
- `sha256`: string optional
- `storage_provider`: string optional

### FileUploadResponse

- `file_id`: uuid
- `status`: string
- `uploaded_at`: string
- `sha256`: string optional

### FileResponse

- `file_id`: uuid
- `url`: string optional
- `metadata`: object

### FileSoftDeleteResponse

- `file_id`: uuid
- `status`: string

### FileLinkCreateRequest

- `file_id`: uuid
- `object_type`: enum `mission|worker|client|compliance_case|a1_case|timesheet|invoice|quote`
- `object_id`: uuid

### FileLinkResponse

- `file_link_id`: uuid
- `file_id`: uuid
- `object_type`: string
- `object_id`: uuid
- `created_at`: string

### CheckEventCreateRequest

- `mission_id`: uuid
- `worker_id`: uuid
- `event_type`: enum `check_in|check_out`
- `occurred_at`: string
- `device_info`: object optional

### CheckEventCreateResponse

- `check_event_id`: uuid
- `recorded_at`: string

## Notes contractuelles

- Login/refresh/reset sont hors de cette API, car geres par Supabase Auth.
- Ne pas ajouter d'endpoints auth custom dans ce patch.
- Ce patch clarifie que les endpoints M3 type `/v1/clients/{client_id}/documents` ne remplacent pas la surface generique `/v1/files`.
- Le stockage concret (Supabase Storage, S3, autre) reste un detail d'implementation hors contrat.
- La liaison probatoire est portee par `file_links` (DB 2.9: `file_links`).

## What remains blocked

- OWNER ARBITRATION REQUIRED: confirmer la granularite finale de `PATCH /v1/users/{user_id}` pour les changements de role.
- CONTRACT GAP: nom d'event pour deactivate/reactivate user non confirme dans 2.10.
- CONTRACT GAP: noms d'events pour create/delete `file-links` non confirmes dans 2.10.

---

## Patch V1.2.2 — Surfaces manquantes (Lots 4, 5, 6, 7, 8) — décisions OWNER 2026-02-20

Decisions OWNER appliquees:
- Q1: invoices:from-timesheet actif V1 (deja dans LOCKED 2.11, aucun ajout ici).
- Q4: client_user elargi sur validation devis, validation timesheets, upload docs vigilance (conditionne a feature flag tenant).
- Q5: RFP unifie M4/M11 via flag visibility (private|public).
- Q6: tracking anti-desintermediation via contact-logs.
- Q9: worker_skills livre V1 (retrait du "(option V1)").
- Q10: endpoint SIPSI structure + upload preuves.
- Q3: Web Push API via service worker (native PWA, aucune dependance FCM).
- Q8: convention nommage code neutre — aucune reference marque dans routes/schemas.

---

### ATS — Job Offers & Applications (M5)

#### POST /v1/job-offers
- Request schema: `JobOfferCreateRequest`
  - title: string
  - sector: string
  - corridor_origin_country: string (PT|RO|PL|FR)
  - corridor_host_country: string (FR)
  - start_date: date
  - end_date: date
  - description: text optional
  - status: draft (default)
- Response schema: `JobOfferResponse` { job_offer_id, tenant_id, status }
- Related Events (2.10): `JobOfferPublished` si status=published

#### PATCH /v1/job-offers/{job_offer_id}/status
- Request schema: `{ "status": "draft|published|closed" }`
- Response schema: `JobOfferResponse`
- Related Events (2.10): `JobOfferPublished` si transition vers published

#### GET /v1/job-offers
- Request schema: none (query params: sector, status, corridor)
- Response schema: `JobOfferListResponse` { items: array, next_cursor: string|null }
- Related Events (2.10): none (read route)

#### POST /v1/applications
- Request schema: `ApplicationCreateRequest`
  - job_offer_id: uuid
  - candidate_id: uuid optional (si parsing CV simultane)
  - source: manual|import|api
- Response schema: `ApplicationResponse` { application_id, status: new }
- Related Events (2.10): `ApplicationReceived`

#### PATCH /v1/applications/{application_id}/status
- Request schema: `{ "status": "screened|shortlisted|approved|rejected|hired" }`
- Response schema: `ApplicationResponse`
- Related Events (2.10): `CandidateScored` si shortlisted avec ai_score

#### GET /v1/applications
- Request schema: none (query params: job_offer_id, status)
- Response schema: `ApplicationListResponse` { items: array, next_cursor: string|null }
- Related Events (2.10): none (read route)

#### POST /v1/applications/{application_id}:shortlist
- Request schema: `{ "shortlist_for_client": boolean }`
- Response schema: `ApplicationResponse`
- Related Events (2.10): none

---

### Workers & Dossiers (M6)

#### POST /v1/workers
- Request schema: `WorkerCreateRequest`
  - first_name: string
  - last_name: string
  - nationality: string
  - language: string (fr|en|pl|ro)
  - email: string optional
  - phone: string optional
- Response schema: `WorkerResponse` { worker_id, tenant_id, status: active }
- Related Events (2.10): `WorkerCreated`

#### GET /v1/workers/{worker_id}
- Request schema: none
- Response schema: `WorkerResponse`
- Related Events (2.10): none (read route)

#### PATCH /v1/workers/{worker_id}
- Request schema: `WorkerPatchRequest` (champs modifiables: phone, address, language, status)
- Response schema: `WorkerResponse`
- Related Events (2.10): none

#### GET /v1/workers/{worker_id}/documents
- Request schema: none
- Response schema: `WorkerDocumentListResponse` { items: array }
- Related Events (2.10): none (read route)

#### POST /v1/workers/{worker_id}/documents
- Note: upload fichier via POST /v1/files + POST /v1/file-links; cet endpoint cree la metadata.
- Request schema: `WorkerDocumentCreateRequest`
  - doc_type: id|passport|work_permit|medical|other
  - valid_from: date optional
  - valid_to: date optional
  - file_id: uuid (issu de POST /v1/files)
- Response schema: `WorkerDocumentResponse` { doc_id, doc_type, doc_status, valid_to }
- Related Events (2.10): `WorkerDocumentStatusChanged`

#### GET /v1/workers/{worker_id}/skills
- Request schema: none
- Response schema: `WorkerSkillListResponse` { items: [{ skill_id, skill_name, level }] }
- Related Events (2.10): none (read route)

#### POST /v1/workers/{worker_id}/skills
- Request schema: `WorkerSkillCreateRequest` { skill_name: string, level: integer 1-5 }
- Response schema: `WorkerSkillResponse` { skill_id, worker_id, skill_name, level }
- Related Events (2.10): `WorkerSkillAdded`

---

### Finance — Devis & Commissions (M10)

#### POST /v1/quotes
- Request schema: `QuoteCreateRequest`
  - client_id: uuid
  - mission_id: uuid optional
  - currency: EUR (default)
  - lines: array de { description: string, quantity: numeric, unit_price: numeric }
- Response schema: `QuoteResponse` { quote_id, quote_number, status: draft, subtotal, tax_total, total }
- Related Events (2.10): `QuoteCreated`
- Regle metier: les devis ne sont JAMAIS bloques par enforcement (SOCLE §2.6).

#### PATCH /v1/quotes/{quote_id}
- Request schema: `QuotePatchRequest` (lignes uniquement si status=draft)
- Response schema: `QuoteResponse`
- Related Events (2.10): none

#### POST /v1/quotes/{quote_id}:send
- Request schema: none
- Response schema: `QuoteResponse` { status: sent }
- Related Events (2.10): `QuoteSent`

#### POST /v1/quotes/{quote_id}:accept
- Request schema: none
- Response schema: `QuoteResponse` { status: accepted }
- Related Events (2.10): `QuoteAccepted`
- Note Q4: client_user autorise si `tenant_settings.modules.client_portal.level = full`.

#### GET /v1/quotes/{quote_id}
- Request schema: none
- Response schema: `QuoteResponse`
- Related Events (2.10): none (read route)

#### GET /v1/invoices/{invoice_id}/commissions
- Request schema: none
- Response schema: `CommissionListResponse` { items: [{ commission_id, consultant_user_id, commission_rate, commission_amount, status }] }
- Related Events (2.10): none (read route)

#### PATCH /v1/commissions/{commission_id}/status
- Request schema: `{ "status": "approved|paid" }`
- Response schema: `CommissionResponse` { commission_id, status }
- Related Events (2.10): `CommissionApproved` si approved

#### POST /v1/accounting-exports
- Request schema: `AccountingExportRequest`
  - export_type: pennylane|sage|ebp|csv
  - periode_start: date
  - periode_end: date
- Response schema: `AccountingExportResponse` { export_id, status: draft }
- Related Events (2.10): no-code prend en charge la generation du fichier via events_outbox

#### GET /v1/accounting-exports/{export_id}
- Request schema: none
- Response schema: `AccountingExportResponse` { export_id, status, file_id optional }
- Related Events (2.10): none (read route)

---

### Compliance Admin — Salary Grids (M8 — decision Q2-B)

#### GET /v1/admin/salary-grids
- Request schema: none (query params: idcc_code, classification_code, effective_from)
- Response schema: `SalaryGridListResponse` { items: [{ grid_id, idcc_code, classification_code, minimum_amount, unit, effective_from, effective_to }] }
- Related Events (2.10): none (read route)

#### POST /v1/admin/salary-grids
- Request schema: `SalaryGridCreateRequest`
  - idcc_code: string
  - classification_code: string
  - minimum_amount: numeric
  - unit: hourly|monthly
  - currency: EUR
  - effective_from: date
  - effective_to: date optional
  - source_ref: string (reference legale: texte ou URL)
- Response schema: `SalaryGridResponse` { grid_id, idcc_code, effective_from }
- Regle: la creation d'une nouvelle version n'ecrase JAMAIS l'ancienne (versioning immuable).
- Related Events (2.10): none

#### POST /v1/admin/mandatory-pay-items
- Request schema: `MandatoryPayItemCreateRequest`
  - idcc_code: string
  - item_code: string
  - label: string
  - calculation_type: fixed|percent|conditional
  - amount_or_rate: numeric
  - unit: EUR|%
  - conditions: jsonb optional
  - effective_from: date
  - effective_to: date optional
- Response schema: `MandatoryPayItemResponse` { item_id, idcc_code, item_code, effective_from }
- Related Events (2.10): none

#### GET /v1/admin/country-rulesets
- Request schema: none (query params: country_code, version)
- Response schema: `CountryRulesetListResponse` { items: [{ ruleset_id, country_code, version, effective_from, effective_to }] }
- Related Events (2.10): none (read route)

---

### RFP Unifie — M4 + M11 (decision Q5 : flag visibility)

Note contractuelle: M4 (RFP interne, Lot 4) et M11 (Marketplace, Lot 8) reposent sur
le meme mecanisme via le flag `visibility`.
- `visibility=private` -> invite manuelle (agences connues, Lot 4)
- `visibility=public` -> diffusion marketplace (agences certifiees, Lot 8)

#### PATCH /v1/rfps/{rfp_id}/visibility
- Request schema: `{ "visibility": "private|public" }`
- Response schema: `RfpResponse` { rfp_id, visibility }
- Prerequis: `visibility=public` exige `marketplace_access.status=active` pour le tenant.
- Erreur si prerequis non rempli: 422 code `marketplace_access_required`.
- Related Events (2.10): `RfpPublished` (deja dans LOCKED 2.10, inchange)

---

### Anti-desintermediation — Contact Tracking (decision Q6 : B)

#### POST /v1/rfps/{rfp_id}/contact-logs
- Request schema: `RfpContactLogRequest`
  - contact_type: email|phone|meeting|platform_message
  - counterpart_tenant_id: uuid (agence ou client contacte)
  - occurred_at: string (ISO datetime)
  - notes: text optional
- Response schema: `RfpContactLogResponse` { log_id, rfp_id, protection_expires_at }
- Regle: `protection_expires_at = occurred_at + 365 jours` (periode de protection 12 mois).
- Related Events (2.10): `RfpContactLogged`

#### GET /v1/rfps/{rfp_id}/contact-logs
- Request schema: none
- Response schema: `RfpContactLogListResponse` { items: array }
- Related Events (2.10): none (read route)

---

### SIPSI — Formulaire structure (decision Q10 : A+B)

Note: SIPSI = Systeme d'Information pour les Prestations de Services Internationaux.
V1 assiste: formulaire structure dans le produit + checklist de preuves a uploader.

#### POST /v1/compliance-cases/{compliance_case_id}/sipsi-declaration
- Request schema: `SipsiDeclarationRequest`
  - employer_name: string
  - employer_country: string (PT|RO|PL|FR)
  - employer_registration_id: string
  - host_site_address: string
  - worker_ids: array of uuid
  - planned_start_date: date
  - planned_end_date: date
  - representative_in_fr_name: string
  - representative_in_fr_contact: string
  - declaration_date: date
- Response schema: `SipsiDeclarationResponse` { declaration_id, status: draft }
- Regle: creation d'un `compliance_requirement` code=`SIPSI_DECLARATION` lie au dossier.
- Related Events (2.10): `ComplianceRequirementCreated` { requirement_code: SIPSI_DECLARATION }

#### POST /v1/sipsi-declarations/{declaration_id}:submit
- Request schema: none
- Response schema: `SipsiDeclarationResponse` { status: submitted }
- Related Events (2.10): `SipsiDeclarationSubmitted` + `ComplianceRequirementStatusChanged` { to: provided }

#### GET /v1/sipsi-declarations/{declaration_id}
- Request schema: none
- Response schema: `SipsiDeclarationResponse`
- Related Events (2.10): none (read route)

---

### Worker App — Lecture mobile etendue (M7bis, decisions Q3-A)

Note: Web Push = standard navigator.serviceWorker + PushManager.
Aucune dependance FCM ou service tiers en V1.

#### GET /v1/worker/missions
- Request schema: none
- Response schema: `WorkerMissionListResponse` { items: [{ mission_id, client_id, site_id, status, start_date, end_date, job_title }] }
- Related Events (2.10): none (read route)

#### GET /v1/worker/missions/{mission_id}
- Request schema: none
- Response schema: `WorkerMissionDetailResponse` { mission_id, status, compliance_status_summary, start_date, end_date }
- Related Events (2.10): none (read route)

#### GET /v1/worker/documents
- Request schema: none
- Response schema: `WorkerDocumentListResponse` { items: [{ doc_id, doc_type, doc_status, valid_to }] }
- Related Events (2.10): none (read route)

#### GET /v1/worker/missions/{mission_id}/remuneration
- Request schema: none
- Response schema: `WorkerRemunerationDeclaredResponse`
  - base_salary_amount: numeric
  - base_salary_frequency: hourly|monthly
  - bonuses: array
  - allowances: array
  - note: "V1 : donnees declarees uniquement, pas de snapshot calcule"
- Related Events (2.10): none (read route)

#### GET /v1/worker/push-subscription
- Request schema: none
- Response schema: `PushSubscriptionResponse` { subscribed: bool, endpoint optional }
- Related Events (2.10): none

#### POST /v1/worker/push-subscription
- Request schema: `PushSubscriptionRequest`
  - endpoint: string (URL service push navigateur)
  - keys: { p256dh: string, auth: string }
- Response schema: `PushSubscriptionResponse` { subscribed: true }
- Related Events (2.10): none

#### DELETE /v1/worker/push-subscription
- Request schema: none
- Response schema: `{ "subscribed": false }`
- Related Events (2.10): none

---

### Schema stubs additionnels (Patch V1.2.2)

#### JobOfferResponse
- job_offer_id: uuid
- tenant_id: uuid
- title: string
- sector: string
- corridor_origin_country: string
- corridor_host_country: string
- status: string

#### ApplicationResponse
- application_id: uuid
- job_offer_id: uuid
- candidate_id: uuid
- status: string
- ai_score: integer optional
- shortlist_for_client: boolean

#### WorkerResponse
- worker_id: uuid
- tenant_id: uuid
- first_name, last_name: string
- nationality: string
- language: string
- status: string

#### WorkerDocumentResponse
- doc_id: uuid
- worker_id: uuid
- doc_type: string
- doc_status: string
- valid_to: date optional

#### WorkerSkillResponse
- skill_id: uuid
- worker_id: uuid
- skill_name: string
- level: integer

#### QuoteResponse
- quote_id: uuid
- quote_number: string
- client_id: uuid
- status: string
- subtotal, tax_total, total: numeric
- currency: string

#### CommissionResponse
- commission_id: uuid
- invoice_id: uuid
- consultant_user_id: uuid
- commission_rate: numeric
- commission_amount: numeric
- status: string

#### AccountingExportResponse
- export_id: uuid
- export_type: string
- status: string
- file_id: uuid optional

#### SalaryGridResponse
- grid_id: uuid
- idcc_code: string
- classification_code: string
- minimum_amount: numeric
- unit: string
- effective_from: date
- effective_to: date optional

#### RfpContactLogResponse
- log_id: uuid
- rfp_id: uuid
- contact_type: string
- counterpart_tenant_id: uuid
- occurred_at: string
- protection_expires_at: string

#### SipsiDeclarationResponse
- declaration_id: uuid
- compliance_case_id: uuid
- status: string
- employer_name: string
- employer_country: string
- planned_start_date: date
- planned_end_date: date

#### PushSubscriptionResponse
- subscribed: boolean
- endpoint: string optional

#### WorkerMissionListResponse
- items: array of { mission_id, status, start_date, end_date, job_title }
- next_cursor: string optional

#### WorkerRemunerationDeclaredResponse
- base_salary_amount: numeric
- base_salary_frequency: string
- bonuses: array
- allowances: array

## Mini-changelog

- 2026-02-19: V1.2.1 owner decisions appliques (Supabase auth, files+linking, endpoint check-events unique), sans modification LOCKED.
- 2026-02-20: V1.2.2 — surfaces M5/M6/M10/M8-admin/RFP/SIPSI/push/mobile etendues. Decisions OWNER Q1 Q3 Q4 Q5 Q6 Q9 Q10 appliquees.
