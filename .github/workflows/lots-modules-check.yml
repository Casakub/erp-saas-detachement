name: lots-modules-check

on:
  pull_request:
  push:

jobs:
  lots-modules-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run SECTION 6 lots/modules anchoring check
        run: bash scripts/notion_lots_modules_check.sh
