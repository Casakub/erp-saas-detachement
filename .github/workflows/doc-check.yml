name: doc-check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  doc-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run doc check
        run: |
          bash scripts/run_doc_check.sh
