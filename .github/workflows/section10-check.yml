name: section10-check

on:
  pull_request:
  push:

jobs:
  section10-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run SECTION 10 completeness check
        run: bash scripts/notion_section10_check.sh
